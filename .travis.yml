language: python

os:
  - linux
  - osx
  - windows

python:
  - "3.5"
  - "3.6"      # current default Python on Travis CI
  - "3.7"
  - "nightly"  # nightly build

install:
  - pip3 install -r requirements.txt

script:
  - ./qmk -h
